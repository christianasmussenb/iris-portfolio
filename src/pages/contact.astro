---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="Contacto | MyAllSupport | IRIS Solutions">
  <style>
    .contact-shell {
      display:grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 14px;
      background: radial-gradient(120% 140% at 15% 20%, rgba(125,211,252,0.22), transparent 50%),
                  radial-gradient(120% 120% at 80% 20%, rgba(167,139,250,0.18), transparent 55%),
                  linear-gradient(135deg, rgba(255,255,255,0.10), rgba(255,255,255,0.02));
      border: 1px solid rgba(255,255,255,0.14);
      border-radius: 22px;
      padding: 18px;
      box-shadow: 0 16px 38px rgba(0,0,0,0.30);
    }
    .contact-card {
      background: rgba(0,0,0,0.08);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 16px;
      padding: 14px;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.06);
    }
    .pill-accent {
      display:inline-flex;
      padding: 6px 12px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.16);
      background: rgba(255,255,255,0.08);
      color: #e8f4ff;
      font-size: 12px;
      letter-spacing: 0.2px;
      text-transform: uppercase;
    }
    .cta-primary {
      display:inline-flex;
      align-items:center;
      gap:10px;
      padding: 12px 16px;
      border-radius: 12px;
      border: none;
      background: linear-gradient(135deg, #9ae6ff, #c7d2fe);
      color: #06111f;
      font-weight: 800;
      box-shadow: 0 12px 26px rgba(0,0,0,0.32);
      text-decoration: none;
      transition: transform 140ms ease, box-shadow 140ms ease, filter 140ms ease;
      width: max-content;
    }
    .cta-primary:hover { transform: translateY(-2px); box-shadow: 0 16px 32px rgba(0,0,0,0.36); filter: brightness(1.04); }
    .form-shell {
      background: rgba(0,0,0,0.10);
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 16px;
      padding: 14px;
      display:flex;
      flex-direction:column;
      gap: 10px;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.06);
    }
    .field {
      width: 100%;
      padding: 12px 12px;
      border-radius: 10px;
      border: 1px solid rgba(255,255,255,0.16);
      background: rgba(0,0,0,0.22);
      color: #e8f4ff;
      font-size: 14px;
    }
    .field::placeholder { color: rgba(255,255,255,0.60); }
    .submit {
      border: none;
      border-radius: 10px;
      padding: 12px;
      background: linear-gradient(135deg, #7dd3fc, #a78bfa);
      color: #06111f;
      font-weight: 800;
      cursor: pointer;
      box-shadow: 0 12px 26px rgba(0,0,0,0.30);
      transition: transform 140ms ease, box-shadow 140ms ease, filter 140ms ease;
    }
    .submit:hover { transform: translateY(-2px); box-shadow: 0 16px 32px rgba(0,0,0,0.34); filter: brightness(1.04); }
  </style>

  <section class="contact-shell">
    <div class="contact-card">
      <div class="pill-accent">Contacto</div>
      <h1 style="font-size: 26px; margin: 12px 0 8px;">Agendemos tu diagnóstico o cuéntanos tu necesidad.</h1>
      <p class="muted" style="line-height:1.7; margin: 0 0 14px;">
        Resolvemos soporte SAP, continuidad tecnológica, ciberseguridad y automatización operativa. Te respondemos pronto.
      </p>
      <a class="cta-primary" href="https://calendly.com/christian-htc/30min?month=2026-01" target="_blank" rel="noreferrer">Agendar 30 min</a>
    </div>

    <form class="form-shell" action="https://formspree.io/f/xeoyrnwz" method="POST">
      <input class="field" type="text" name="name" placeholder="Tu nombre" required />
      <input class="field" type="email" name="email" placeholder="Tu email" required />
      <input class="field" type="text" name="company" placeholder="Empresa" />
      <textarea class="field" name="message" rows="4" placeholder="Cuéntanos tu reto" required></textarea>
      <button class="submit" type="submit">Enviar mensaje</button>
    </form>
  </section>
</BaseLayout>
